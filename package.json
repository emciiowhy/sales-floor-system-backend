{
  "name": "sales-floor-system-backend-root",
  "version": "1.0.0",
  "description": "Root package.json for Sales Floor System Backend",
  "type": "module",
  "scripts": {
    "postinstall": "cd backend && npm run prisma:generate",
    "build": "cd backend && npm run prisma:deploy",
    "start": "cd backend && npm start",
    "dev": "cd backend && npm run dev"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "prisma": "^5.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "prisma": {
    "seed": "node backend/prisma/seed.js"
  }
}
